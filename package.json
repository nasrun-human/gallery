{
  "name": "gallery-app",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install && npm install --prefix web",
    "build-web": "npm run build --prefix web",
    "start": "node server/index.js",
    "render-postbuild": "npm run install-all && npm run build-web"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.0.2",
    "sqlite3": "^5.1.7"
  }
}
